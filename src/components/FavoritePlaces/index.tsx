import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const FavoritePlaces = (props: Props) => {
  return (
    <View>
      <Text>index</Text>
    </View>
  );
};

export default FavoritePlaces;

const styles = StyleSheet.create({});
